{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "input-date-picker",
  "title": "Input Date Picker",
  "description": "A date picker component with a dropdown selection interface.",
  "dependencies": ["react-day-picker", "date-fns"],
  "registryDependencies": [
    "button",
    "calendar",
    "label",
    "popover",
    "separator"
  ],
  "files": [
    {
      "path": "registry/helisa/ui/input-date-picker.tsx",
      "content": "'use client';\n\nimport { Button } from '@/components/ui/button';\nimport { Calendar } from '@/components/ui/calendar';\nimport { Label } from '@/components/ui/label';\nimport { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover';\nimport { Separator } from '@/components/ui/separator';\nimport { format } from 'date-fns';\n\nimport { ChevronDownIcon } from 'lucide-react';\nimport React from 'react';\nimport { DayPickerProps } from 'react-day-picker';\nimport { es } from 'react-day-picker/locale';\n\nexport const InputDatePicker = (\n  calendarProps: DayPickerProps & {\n    buttonVariant?: React.ComponentProps<typeof Button>['variant'];\n  } & { label?: string; clearEnable?: boolean; onClear?: () => void; dateFormat?: string },\n) => {\n  const [open, setOpen] = React.useState(false);\n  const dateFormat = calendarProps.dateFormat || 'yyyy/MM/dd';\n  const handleClear = () => {\n    if (calendarProps.onClear) {\n      calendarProps.onClear();\n    }\n    setOpen(false);\n  };\n  return (\n    <div className=\"flex flex-col gap-3\">\n      {calendarProps.label && (\n        <Label htmlFor=\"date\" className=\"px-1\">\n          {calendarProps.label}\n        </Label>\n      )}\n\n      <Popover open={open} onOpenChange={setOpen}>\n        <PopoverTrigger asChild>\n          <Button variant=\"outline\" id=\"date\" className=\"justify-between font-normal w-fit\">\n            {(() => {\n              switch (calendarProps.mode) {\n                case 'single':\n                  return calendarProps.selected ? (\n                    format(calendarProps.selected, dateFormat)\n                  ) : (\n                    <span className=\"text-muted-foreground\">Selecciona una fecha</span>\n                  );\n                case 'multiple':\n                  return calendarProps.selected && calendarProps.selected.length > 0 ? (\n                    `${calendarProps.selected.length} fechas seleccionadas`\n                  ) : (\n                    <span className=\"text-muted-foreground\">Selecciona las fechas</span>\n                  );\n                case 'range':\n                  if (calendarProps.selected?.from) {\n                    return calendarProps.selected.to\n                      ? `${format(calendarProps.selected.from, dateFormat)} - ${format(calendarProps.selected.to, dateFormat)}`\n                      : `${format(calendarProps.selected.from, dateFormat)} - ...`;\n                  }\n                  return <span className=\"text-muted-foreground\">Selecciona rango de fechas</span>;\n                default:\n                  return <span className=\"text-muted-foreground\">Selecciona una fecha</span>;\n              }\n            })()}\n            <ChevronDownIcon />\n          </Button>\n        </PopoverTrigger>\n        <PopoverContent className=\"w-auto overflow-hidden p-0\" align=\"start\">\n          <Calendar mode=\"single\" locale={es} captionLayout=\"dropdown\" {...calendarProps} />\n          {calendarProps.clearEnable && (\n            <>\n              <Separator />\n              <Button variant=\"ghost\" className=\"w-full rounded-none rounded-b-md\" onClick={handleClear}>\n                Limpiar\n              </Button>\n            </>\n          )}\n        </PopoverContent>\n      </Popover>\n    </div>\n  );\n};\n\nexport default InputDatePicker;\n",
      "type": "registry:ui",
      "target": "components/helisa/ui/input-date-picker.tsx"
    }
  ],
  "type": "registry:ui"
}
